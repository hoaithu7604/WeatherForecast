import {StyleSheet} from 'react-native';
import GLOBALS from '../../Globals';
export default StyleSheet.create({
    container:{
        flex:3,
        width:'100%',
        alignItems: 'center',
        backgroundColor: '#fff',
        margin:5,
        zIndex:-1,
    },
})