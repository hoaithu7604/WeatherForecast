import {StyleSheet} from 'react-native';
import GLOBALS from '../../Globals';
export default StyleSheet.create({
    container:{
        flex:4,
        width:'100%',
        alignItems: 'center',
        backgroundColor: '#000',
        margin:5,
        zIndex: -1,
    },
})